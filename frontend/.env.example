# Normal backend (REST API, auth, WS ticket issuance)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Realtime server (Socket.IO) — deployed separately from the REST API.
# Production: https://algoriumx.realtime.pramit.tech
NEXT_PUBLIC_REALTIME_URL=http://localhost:8001
